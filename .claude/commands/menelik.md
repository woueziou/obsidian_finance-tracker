---
description: Invoke menelik — the implementation agent for the Obsidian Finance Tracker. Pass a phase number or task description (e.g. /menelik "start phase 1" or /menelik "implement the expense parser").
---

Invoke the menelik subagent with the following task: $ARGUMENTS

If no arguments are given, ask the user which phase or task they want to work on before starting.