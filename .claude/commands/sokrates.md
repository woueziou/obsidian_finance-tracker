---
description: Invoke sokrates — the pre-flight agent for the Obsidian Finance Tracker. Run before menelik starts a phase to verify plan/codebase coherence (e.g. /sokrates "phase 3" or /sokrates "check phase 3 before starting").
---

Invoke the sokrates subagent with the following task: $ARGUMENTS

If no arguments are given, ask the user which phase they want pre-flight checked before starting.
